<MahApps:MetroWindow x:Class="Badger.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:MahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="300" Width="300">
    <Grid>
        <Button x:Name="ConfigureSettings" HorizontalAlignment="Right" Height="50" Width="50"  VerticalAlignment="Top" Style="{DynamicResource MetroCircleButtonStyle}">
            <Rectangle Fill="Gray" Height="30" Width="30">
                 <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_settings}"/>
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>
    </Grid>
</MahApps:MetroWindow>
